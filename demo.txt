sudo nmap -sn 192.168.1.0/24

ssh pi@192.168.1.101
raspberry

sudo nano /etc/rc.local
ps -aux | grep python3
sudo kill <pid>

sudo strace -pPID -s9999 -e write

Relay

- 3v -> to VCC of relay
- 5v -> to JP_VCCC of relay
- low -> relay OPEN

cd pi/
python3 -m venv env
source env/bin/activate

python3 -m unittest tests.test_main


BME280
1.7-3.6V
1.6 uA
W ?
R ?

SCL -> GPIO3
SDA -> GPIO2


DS18B20
3-5.5 V
1-1.5mA
W ?
R ?

sudo nano /boot/config.txt
add dtoverlay=w1-gpio (at the bottom of the file)

sudo modprobe w1-gpio
sudo modprobe w1-therm

GND
DQ -> 4.7k -> Vdd -> GPIO4
Vdd 

PH SENSOR
5V
GND 
CH0 MCP3008

>> MCP3008
dtparam=spi=on (enable SPI in /boot/config.txt)
- V3.3
- 






- Regultae ph Sensor
- Use new temperature Sensor
- control FANs (PWm & ON/OFF)
- Control Light
- Use EC sensor